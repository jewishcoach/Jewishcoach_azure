[pytest]
# Async tests use `async def` without @pytest.mark.asyncio; pytest-asyncio 0.24+
# defaults to strict mode otherwise (breaks CI `pytest tests/`).
asyncio_mode = auto
markers =
    llm: calls external LLM classifier(s); output-dependent (run manually or with secrets)
