# Deploy backend (FastAPI) to Azure Web App
# Required: AZURE_WEBAPP_PUBLISH_PROFILE (from Azure Portal → jewishcoach-api → Get publish profile)
# Optional: AZURE_CREDENTIALS - if set, restarts app before deploy to clear 409 lock
#   Create: az ad sp create-for-rbac --name "github-deploy" --role contributor --scopes /subscriptions/<SUB_ID>/resourceGroups/jewish-coach-rg --sdk-auth
#   Add to GitHub: Settings → Secrets → AZURE_CREDENTIALS (paste JSON)

name: Deploy Backend to Azure Web App

# Queue deploys - do NOT cancel. Cancelling leaves Azure in locked state → 409 on next deploy.
concurrency:
  group: backend-deploy-${{ github.ref }}
  cancel-in-progress: false

on:
  push:
    branches:
      - main
    paths:
      - 'backend/**'
      - '.github/workflows/azure-backend-deploy.yml'
  workflow_dispatch:

env:
  AZURE_WEBAPP_NAME: jewishcoach-api
  RESOURCE_GROUP: jewish-coach-rg

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    name: Build and Deploy Backend

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: backend/requirements.txt

      - name: Install backend dependencies
        run: pip install -r backend/requirements.txt

      - name: Validate prompt pipeline
        run: |
          python3 test_prompt_manager.py
          python3 test_prompt_pipeline_local.py
          python3 test_prompts_simple.py

      - name: Create deploy zip
        run: |
          cd backend
          python3 -c "
          import zipfile, os
          with zipfile.ZipFile('deploy.zip', 'w', zipfile.ZIP_DEFLATED) as z:
              for path in ['app', 'requirements.txt', 'startup.sh', 'gunicorn_conf.py', 'web.config']:
                  if os.path.isdir(path):
                      for r, d, f in os.walk(path):
                          for fn in f:
                              if '__pycache__' in r or fn.endswith('.pyc') or 'antenv' in r or 'test_venv' in r:
                                  continue
                              z.write(os.path.join(r, fn))
                  elif os.path.isfile(path):
                      z.write(path)
          "
          ls -la deploy.zip

      - name: Restart app (clear 409 lock) - requires AZURE_CREDENTIALS
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
        continue-on-error: true

      - name: Restart app (clear 409 lock)
        run: |
          az webapp restart --name jewishcoach-api --resource-group jewish-coach-rg
          echo "Waiting 60s for app to stabilize..."
          sleep 60
        continue-on-error: true

      - name: Wait for any previous deployment to finish
        run: sleep 180

      - name: Deploy to Azure Web App
        id: deploy
        uses: azure/webapps-deploy@v2
        with:
          app-name: ${{ env.AZURE_WEBAPP_NAME }}
          package: backend/deploy.zip
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}

  # Retry deploy on 409 Conflict (common transient error)
  retry-deploy:
    needs: build-and-deploy
    if: failure()
    runs-on: ubuntu-latest
    name: Retry Deploy (on 409)
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: echo "First deploy failed (likely 409). Waiting 3 min before retry..."
      - run: sleep 180
      - name: Create deploy zip
        run: |
          cd backend
          python3 -c "
          import zipfile, os
          with zipfile.ZipFile('deploy.zip', 'w', zipfile.ZIP_DEFLATED) as z:
              for path in ['app', 'requirements.txt', 'startup.sh', 'gunicorn_conf.py', 'web.config']:
                  if os.path.isdir(path):
                      for r, d, f in os.walk(path):
                          for fn in f:
                              if '__pycache__' in r or fn.endswith('.pyc') or 'antenv' in r or 'test_venv' in r:
                                  continue
                              z.write(os.path.join(r, fn))
                  elif os.path.isfile(path):
                      z.write(path)
          "
      - name: Retry Deploy to Azure Web App
        id: retry1
        uses: azure/webapps-deploy@v2
        continue-on-error: true
        with:
          app-name: jewishcoach-api
          package: backend/deploy.zip
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}

      - name: Second retry (if first retry failed)
        if: steps.retry1.outcome == 'failure'
        run: sleep 120
      - name: Final Deploy
        if: steps.retry1.outcome == 'failure'
        uses: azure/webapps-deploy@v2
        with:
          app-name: jewishcoach-api
          package: backend/deploy.zip
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
